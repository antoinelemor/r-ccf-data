% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{ccf_search}
\alias{ccf_search}
\title{Unified search over the CCF corpus.}
\usage{
ccf_search(
  client,
  query,
  level = "sentence",
  mode = "text",
  filters = list(),
  thresholds = NULL,
  filter_timing = "pre",
  hybrid_weight = 0.5,
  page_size = 100L,
  limit = NULL,
  raw = FALSE,
  corpus = NULL
)
}
\arguments{
\item{client}{A \code{ccf_client}.}

\item{query}{Search text. Use \code{"*"} or empty to enter \code{"browse"} mode.}

\item{level}{\code{"sentence"} (default — one row per matching sentence
with highlighted \code{headline}) or \code{"article"} (aggregated per \code{doc_id}
with frame percentages and \code{matching_count}).}

\item{mode}{One of:
\itemize{
\item \code{"text"} — Postgres FTS with language-aware stemming (default).
\item \code{"keyword"} — exact \code{ILIKE} substring match (no stemming).
\item \code{"semantic"} — FAISS dense retrieval over BAAI/bge-m3 embeddings.
\item \code{"hybrid"} — blend of FTS + semantic (\code{hybrid_weight}).
\item \code{"entity"} — search inside the NER fields.
\item \code{"browse"} — apply filters only (no query).
\item \code{"cascade_xref"} / \code{"event_xref"} — limit to overlap with cascades / clusters.
}}

\item{filters}{Named list of server-side filters; common keys:
\code{lang} (\code{"en"} / \code{"fr"}, case-insensitive), \code{media} (string or
character vector of outlets), \code{date_from}, \code{date_to} (ISO),
\code{frames}, \code{tone}, \code{subcategories}, \code{messenger_subs},
\code{event_subs}, \code{solutions}, \code{front_page}, \code{urgency}, \code{canada},
\code{news_type}, \code{author}, \code{words_count_min}, \code{words_count_max},
\code{province} (auto-translated to a media list).}

\item{thresholds}{Optional list-of-lists: per-article annotation
thresholds, e.g. \code{list(list(column = "economic_frame", min_pct = 0.3))}.}

\item{filter_timing}{\code{"pre"} (default — applied during SQL) or
\code{"post"} (apply annotation filters in-memory after a wider fetch).}

\item{hybrid_weight}{Numeric in \verb{[0, 1]} blending semantic vs FTS
when \code{mode = "hybrid"}.}

\item{page_size}{Server page size; the client auto-paginates.}

\item{limit}{Cap rows returned (\code{NULL} = fetch every page).}

\item{raw}{If \code{TRUE} return a list with the raw rows + last response;
otherwise return a tibble (default).}
}
\value{
A tibble (or a list when \code{raw = TRUE}).
}
\description{
Wraps \code{POST /api/search/advanced}. Auto-paginates results until
\code{limit} rows have been collected (or all pages have been fetched).
}
\section{Tier}{

Minimum: \code{researcher}. The search itself counts against the daily
\code{searches_remaining} quota in addition to the regular request quota.
}

\examples{
\dontrun{
df <- ccf_search(ccf, "carbon tax", level = "sentence",
                 filters = list(lang = "en", date_from = "2015-01-01"),
                 limit = 500)
}
}
