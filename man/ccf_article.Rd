% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{ccf_article}
\alias{ccf_article}
\title{Full article: metadata + every sentence + per-sentence annotations.}
\usage{
ccf_article(client, doc_id, corpus = NULL)
}
\arguments{
\item{client}{A \code{ccf_client}.}

\item{doc_id}{Integer document ID.}
}
\value{
Named list parsed from JSON.
}
\description{
Hits \verb{GET /api/article/<doc_id>}. Returns a list with article-level
fields (\code{title}, \code{author}, \code{media}, \code{date}, \code{language}, \code{news_type},
\code{front_page}, ...) and a \code{sentences} list, one entry per sentence
with all annotation columns.
}
\section{Tier}{

Minimum: \code{researcher}. Viewer accounts (separate from the tier system)
may still hit a per-account article-view quota; once exhausted, the
server returns sentences with empty \code{sentence_text} and sets
\code{viewer_limit_reached = TRUE}.
}

\examples{
\dontrun{
art <- ccf_article(ccf, 123456L)
art$title; length(art$sentences)
}
}
