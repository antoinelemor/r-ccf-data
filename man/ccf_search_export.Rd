% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{ccf_search_export}
\alias{ccf_search_export}
\title{Server-side CSV export of a search query, parsed back into a tibble.}
\usage{
ccf_search_export(
  client,
  query,
  filters = list(),
  columns = NULL,
  max_rows = 50000L,
  mode = "text",
  include_search_params = FALSE,
  to_tibble = TRUE,
  corpus = NULL
)
}
\arguments{
\item{client}{A \code{ccf_client}.}

\item{query}{Search text (uses Postgres FTS regardless of \code{mode}).}

\item{filters}{Named list of filters; same keys as \code{\link[=ccf_search]{ccf_search()}}.}

\item{columns}{Character vector of columns to include
(\code{NULL} = all whitelisted). See \code{EXPORT_COLUMNS_WHITELIST} server-side.}

\item{max_rows}{Maximum rows to return (server cap, default 50,000).}

\item{mode}{Search mode label (logged with the export).}

\item{include_search_params}{If \code{TRUE}, prepend a \verb{# Query:} header
block at the top of the CSV.}

\item{to_tibble}{If \code{TRUE} (default) parse the CSV into a tibble;
\code{FALSE} returns the raw CSV string.}
}
\value{
A tibble (or character scalar when \code{to_tibble = FALSE}).
}
\description{
Hits \code{POST /api/search/export}. The export endpoint enforces a
per-token daily export quota (in addition to the regular request
quota), so each call also decrements \code{exports_remaining}.
}
\section{Tier}{

Minimum: \code{expert}. Lower tiers will receive a \code{ccf_tier_error}.
}

\examples{
\dontrun{
df <- ccf_search_export(ccf, "carbon tax",
  filters = list(lang = "en"),
  columns = c("doc_id","sentence_text","pub_date","media","dominant_frame"))
}
}
