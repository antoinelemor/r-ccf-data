% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{ccf_distribution}
\alias{ccf_distribution}
\title{Aggregate annotation counts grouped by year/month/media/language.}
\usage{
ccf_distribution(
  client,
  columns,
  group_by = "year",
  lang = NULL,
  media = NULL,
  date_from = NULL,
  date_to = NULL,
  raw = FALSE,
  corpus = NULL
)
}
\arguments{
\item{client}{A \code{ccf_client}.}

\item{columns}{Character vector of annotation column names. Pick
from \code{\link[=ccf_codebook]{ccf_codebook()}}'s \code{definitions}, e.g.
\code{c("economic_frame", "health_frame")}.}

\item{group_by}{One of \code{"year"}, \code{"month"}, \code{"media"}, \code{"language"}.}

\item{lang}{Optional language filter, \code{"en"} / \code{"fr"}
(case-insensitive — normalized to uppercase before sending).}

\item{media}{Optional single media-outlet filter (string).}

\item{date_from, date_to}{ISO date strings for the date window.}

\item{raw}{If \code{TRUE} return the raw response list (with \code{data},
\code{group_by}, \code{columns}); default returns the tibble of \code{data}.}
}
\value{
A tibble (or list when \code{raw = TRUE}).
}
\description{
Hits \code{GET /api/distribution}. Returns a tibble with one row per
group and one column per requested annotation; the
\code{sentence_count} and \code{article_count} columns are always added.
}
\section{Tier}{

Minimum: \code{analyst}.
}

\examples{
\dontrun{
df <- ccf_distribution(ccf,
  columns  = c("economic_frame", "health_frame"),
  group_by = "year", lang = "en")
}
}
